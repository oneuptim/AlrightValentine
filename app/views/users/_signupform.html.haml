/ adding, 'remote => true' will make this form submit AJAX rather than rails
= form_for @user  do |f|
  - if @user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user.errors.count, "error")} prohibited this user from being saved:"
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :username
    = f.text_field :username
  .field
    = f.label :email
    = f.text_field :email
  .field
    = f.label :password
    = f.password_field :password
  .field
    = f.label :password_confirmation
    = f.password_field :password_confirmation
  .field
    = f.label :gender
    = f.text_field :gender
  .field
    = f.label :age
    = f.number_field :age
  .field
    = f.label :location
    = f.text_field :location
  .field
    = f.label "Gender Preferences"
    = f.text_field :genderpreferences
  .field
    = f.label :hobbies
    = f.text_field :hobbies
  .field
    = f.label :about
    = f.text_area :about
  .actions
    = f.submit 'Save'